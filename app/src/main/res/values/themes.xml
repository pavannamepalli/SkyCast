<resources >
    <!-- Base application theme. -->
    <style name="Theme.SkyCast" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/matte_black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/weather_icon</item>
        <item name="windowSplashScreenAnimationDuration">150</item>
        <item name="postSplashScreenTheme">@style/Theme.SkyCast</item>
    </style>

    <style name="CustomSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Set the text color for the SearchView -->
        <item name="android:textColorHint">@color/matte_black</item>
        <item name="android:textColor">@color/matte_black</item>


    </style>

</resources>